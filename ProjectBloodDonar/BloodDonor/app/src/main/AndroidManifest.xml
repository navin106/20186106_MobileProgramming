<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blooddonor">
    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/frontlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".messageforrequest"></activity>
        <activity android:name=".seeblood" />
        <activity
            android:name=".RecipientIndividual"
            android:label="Post Blood Request" />
        <activity android:name=".Recipientsignup" />
        <activity android:name=".profile" />
        <activity
            android:name=".Individual"
            android:label="Blood Donation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DonorLogin" />
        <activity android:name=".RecipientLogin" />
        <activity android:name=".Donorsignup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>